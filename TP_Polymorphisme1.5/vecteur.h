/**
   @file vecteur.h
   @brief Déclaration de la classe Vecteur
   @version 1.0
   @author Axel DOMINGOS
   @date 15/10/2020
  */
/*
#ifndef VECTEUR_H
#define VECTEUR_H
#include "element.h"
#include <algorithm>
#include <iostream>
#include <iomanip>
#include <fstream>
#include <string>

class Vecteur : public Element
{
private:
    double x;
    double y;
public:
    Vecteur(const double _x, const double _y);
    Vecteur Operator (const char *_autre);
    void Afficher();
    void setX(const double _x);
    double getX();
    void setY(const double _y);
    double getY();
};

#endif // VECTEUR_H

*/
